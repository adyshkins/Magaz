<Window x:Class="Magaz.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Magaz"
        mc:Ignorable="d"
        Title="Window1" Height="200" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding ProductName}"/>
            <TextBlock Text="{Binding ProductDescription}"/>
        </Grid>

        <Border Grid.Column="0" Margin="3" BorderBrush="Gray" BorderThickness="1" CornerRadius="4">
            <Image x:Name="productImage" />
        </Border>

        <TextBlock Grid.Column="2" Text="{Binding ProductPrice}"/>
    </Grid>
</Window>
